import React from 'react';

// Fix: Per Gemini API guidelines, UI for API key management is not permitted.
// The API key must be provided via the `process.env.API_KEY` environment variable.
// This component has been neutralized to comply with the guidelines.
export default function ApiKeyModal() {
  return null;
}
